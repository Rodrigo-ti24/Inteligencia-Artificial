### GET /health
GET http://localhost:8000/health

### GET /
GET http://localhost:8000/

### POST Pregunta
POST http://localhost:8000/generate
Content-Type: application/json

{
    "prompt": "hazme una pregunta de entrevista de trabajo",
    "system_prompt": "solo muestra la pregunta",
    "temperature": 0.7,
    "max_tokens": 100
}

### Post handleEvaluate
POST http://localhost:8000/evaluateAnswer
Content-Type: application/json

{
    "prompt": "¿como te vez en 5 años? igual de rico",
    "system_prompt": "dale una calificacion entre 0 y 100 a la respuesta. responde con una respuesta mas apropiada para la entrevista de trabajo. solo muestra la respuesta",
    "temperature": 0.7,
    "max_tokens": 100
}